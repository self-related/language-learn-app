// TODO

/*

export function loadStateFromLocalStorage<T>(key: string, initialState: T) {
    const localStorageValue = localStorage.getItem(key);
    const parsedValue = localStorageValue ? JSON.parse(localStorageValue) : null;
}

*/